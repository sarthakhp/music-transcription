# FastAPI Backend Configuration
# Copy this file to .env and update with your settings

# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# Storage Configuration
STORAGE_PATH=./storage
MAX_FILE_SIZE_MB=100
JOB_RETENTION_DAYS=7

# Processing Configuration
MAX_CONCURRENT_JOBS=3
ENABLE_GPU=true

# Database Configuration
# For development (SQLite)
DATABASE_URL=sqlite:///./api.db

# For production (PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/musicapi

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:60151

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=./logs/api.log

# Pipeline Model Paths (relative to project root)
SEPARATION_MODEL_PATH=models/separation
CHORD_MODEL_PATH=models/chord_detection/btc_model.pt

# Remote Publishing (Firebase Storage) — for the hosted, view-anywhere viewer.
# Leave unset for local-only use (publishing is then skipped).
# FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json
# FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
# PUBLISH_ON_COMPLETE=true

